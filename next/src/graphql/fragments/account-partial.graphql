fragment AccountPartial on Account {
  access_token
  expires_at
  id
  id_token
  userId
  oauth_token
  oauth_token_secret
  provider
  providerAccountId
  refresh_secret
  refresh_token
  scope
  session_state
  token_type
  type
  __typename
}
